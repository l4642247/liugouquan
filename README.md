## liugouquan

一个使用 **Cloudflare Pages** 部署的极简静态页面示例。

- 部署平台：Cloudflare Pages  
- 项目名称：`liugouquan`  
- 主要文件：根目录下的 `index.html`  

要在本地预览：

```bash
npx wrangler pages dev .
```

要手动部署到 Cloudflare Pages：

```bash
npx wrangler pages deploy . --project-name=liugouquan
```


